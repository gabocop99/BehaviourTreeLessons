using UnityEngine;

public class Bullet : MonoBehaviour
{
    private void Update()
    {
       // Move();
    }

    private void Move(Vector3 direction)
    {
        var dir = direction.normalized;
        //transform.position
    }
}
